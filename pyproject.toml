[build-system]
requires = ["setuptools>=68.0", "wheel"]
build-backend = "setuptools.backends.legacy:build"

[project]
name = "cfp-audit-intelligence"
version = "0.2.0"
description = "Plataforma de auditoría inteligente de actas del Consejo Federal Pesquero de Argentina"
readme = "README.md"
requires-python = ">=3.10"
license = { text = "MIT" }
keywords = ["fisheries", "argentina", "audit", "nlp", "transparency"]

[tool.ruff]
line-length = 100
target-version = "py310"

[tool.ruff.lint]
select = ["E", "F", "I", "UP"]
ignore = ["E501"]

[tool.ruff.lint.per-file-ignores]
# Scripts manipulate sys.path before imports — E402 is intentional
"scripts/*.py" = ["E402"]
# Streamlit pages do conditional imports after st.stop() guards — E402 is intentional
"src/dashboard/pages/*.py" = ["E402"]

[tool.pytest.ini_options]
testpaths = ["tests"]
python_files = ["test_*.py"]
python_classes = ["Test*"]
python_functions = ["test_*"]
