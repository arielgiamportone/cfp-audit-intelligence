# Dependencias para despliegue en producción (sin torch).
# Usa onnxruntime para sentence-transformers en lugar de PyTorch.
# Ahorra ~2 GB de imagen Docker.
#
# Instalación:
#   pip install -r requirements-deploy.txt
#   pip install sentence-transformers --no-deps
#   pip install onnxruntime  # para embeddings sin GPU

# ── Core web & scraping ──────────────────────────────────────────
streamlit>=1.36.0
requests>=2.31.0
beautifulsoup4>=4.12.0
lxml>=4.9.0
urllib3>=2.0.0
httpx>=0.27.0
fastapi>=0.111.0
uvicorn[standard]>=0.30.0

# ── Data & storage ────────────────────────────────────────────────
pandas>=2.1.0
numpy>=1.26.0
pydantic>=2.5.0
pydantic-settings>=2.1.0
python-dotenv>=1.0.0

# ── PDF processing ────────────────────────────────────────────────
pdfplumber>=0.10.0
PyMuPDF>=1.23.0
pytesseract>=0.3.10
Pillow>=10.0.0

# ── NLP (sin torch — usa onnxruntime) ─────────────────────────────
spacy>=3.7.0
# sentence-transformers se instala sin deps y se provee onnxruntime:
#   pip install sentence-transformers --no-deps && pip install onnxruntime
onnxruntime>=1.16.0

# ── Vector database ───────────────────────────────────────────────
chromadb>=0.4.22

# ── Graph analysis ────────────────────────────────────────────────
networkx>=3.2.0
pyvis>=0.3.2

# ── AI / LLM ─────────────────────────────────────────────────────
anthropic>=0.28.0

# ── Visualization ─────────────────────────────────────────────────
plotly>=5.18.0

# ── Report generation ─────────────────────────────────────────────
reportlab>=4.0.0

# ── Utilities ─────────────────────────────────────────────────────
tqdm>=4.66.0
loguru>=0.7.0
tenacity>=8.2.0
PyYAML>=6.0.0
python-dateutil>=2.8.0
